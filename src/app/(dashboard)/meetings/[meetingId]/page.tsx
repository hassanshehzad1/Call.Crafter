/**
 * The function `page` returns a JSX element displaying "Meeting Id".
 * @returns A React functional component named `page` is being returned. The component renders a
 * `<div>` element with the text "Meeting Id" inside it.
 */

const page = () => {
  return (
    <div>
      Meeting Id
    </div>
  )
}

export default page
